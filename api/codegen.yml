overwrite: true
schema: "http://localhost:4000/graphql"
documents: null
generates:
  ./src/resolver-types.ts:
    config:
      noNamespaces: true
    plugins:
      - "typescript-common"
      - "typescript-server"
      - "typescript-resolvers"