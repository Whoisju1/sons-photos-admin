// import { ResolverFn } from 'apollo-server-express';

// export interface IArgs {
//   to: string;
//   content: string;
//   subject: string;
// }

// export const sendEmail: ResolverFn = async (root, args: { input: IArgs }, context) => {
//   try {
//     const response = await context.emailService.sendMail(args.input);
//     return response;
//   } catch (e) {
//     return e;
//   }
// };
