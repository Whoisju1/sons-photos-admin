import React from 'react';
import styled from ' ../../styled-components';

const StyledNotFound = styled.div``;

const NotFound = () => <StyledNotFound>Page Not Found</StyledNotFound>;

export default NotFound;
