version: '3'
services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile.dev
    ports:
      - "4000:4000"
    volumes:
      - ./api:/api
      - /api/node_modules
    depends_on:
      - postgres
    links:
      - postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: sons_photos
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5431
  client:
    build:
      context: ./client
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./client:/client
      - /client/node_modules
  postgres:
    build:
      context: ./postgres
      dockerfile: Dockerfile.dev
    ports:
      - "5431:5432"
    volumes:
      - postgres_database:/var/lib/postgresql/data:Z
    env_file: ./postgres/postgres.env
# run this command to create volume: "docker volume create postgres_database"
volumes:
    postgres_database:
        external: true