# ROOT TYPES
type Query {
  account: Account
  galleries: [Gallery]
  gallery: Gallery
  photo: Photo
}

type Mutation {
  createAccount(input: createAccountInput): Account
}

# TYPES
type Account {
  accountID: ID!
  firstName: String!
  lastName: String!
  username: String!
  email: String
  phone: Int
  createdAt: String
  company: Company
}

type Photo {
  photoID: ID!
  url: String
  description: String
  clickCount: Int
  created_at: String
}

type Gallery {
  galleryID: ID!
  title: String!
  description: String
  clickCount: Int
  createdAt: String
  photos: [Photo]
}

type Company {
  companyID: ID!
  name: String!
  logo: String
  motto: String
  email: String
  phone: Int
  description: String
}

# INPUT TYPES 
input createAccountInput {
  username: String!
  first_name: String!
  last_name: String!
  password: String!
  email: String
  phone: Int
}